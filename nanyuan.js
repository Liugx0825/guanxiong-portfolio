const view=document.querySelector('#lightbox');const zoomButton=document.createElement('button');zoomButton.id='detail-scale';zoomButton.type='button';zoomButton.textContent='1:1 / Fit';zoomButton.setAttribute('aria-label','Toggle original image size');zoomButton.setAttribute('aria-pressed','false');view.append(zoomButton);zoomButton.addEventListener('click',()=>{const expanded=view.classList.toggle('native-size');zoomButton.setAttribute('aria-pressed',String(expanded))});view.addEventListener('close',()=>{view.classList.remove('native-size');zoomButton.setAttribute('aria-pressed','false')});