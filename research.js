const researchView=document.querySelector('#lightbox');const scaleButton=document.createElement('button');scaleButton.id='research-scale';scaleButton.type='button';scaleButton.textContent='1:1 / Fit';scaleButton.setAttribute('aria-label','Toggle original image size');scaleButton.setAttribute('aria-pressed','false');researchView.append(scaleButton);scaleButton.addEventListener('click',()=>{const expanded=researchView.classList.toggle('native-size');scaleButton.setAttribute('aria-pressed',String(expanded))});researchView.addEventListener('close',()=>{researchView.classList.remove('native-size');scaleButton.setAttribute('aria-pressed','false')});